version: "3"

networks:
  backend:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.15.0.1/24

volumes:
  postgres_data:
  es_data:
  node_rpc_data:
  node_validator_data:
